# 🚀 Инструкция по развертыванию проекта на Vercel

Этот проект состоит из двух частей:
- **Frontend**: Next.js приложение (деплоится на Vercel)
- **Backend**: Django REST API (требуется отдельный хостинг)

## 📋 Предварительные требования

1. Аккаунт на [Vercel](https://vercel.com)
2. Аккаунт на платформе для бэкенда (Railway, Render, Heroku и т.д.)
3. Git репозиторий с вашим кодом

## 🔧 Шаг 1: Подготовка бэкенда (Django)

**Важно**: Бэкенд должен быть развернут ПЕРЕД развертыванием фронтенда.

### Вариант 1: Railway (рекомендуется)

1. Перейдите на [Railway](https://railway.app)
2. Создайте новый проект
3. Подключите ваш репозиторий
4. Выберите папку `backend` в качестве root directory
5. Railway автоматически определит Django проект
6. Добавьте переменные окружения:
   - `SECRET_KEY` - ваш секретный ключ Django
   - `DEBUG=False`
   - `ALLOWED_HOSTS=your-app.railway.app`
   - `DATABASE_URL` - Railway автоматически создаст PostgreSQL
7. После деплоя скопируйте URL вашего API (например: `https://your-app.railway.app`)

### Вариант 2: Render

1. Перейдите на [Render](https://render.com)
2. Создайте новый Web Service
3. Подключите репозиторий
4. Настройки:
   - Build Command: `cd backend && pip install -r requirements.txt && python manage.py migrate`
   - Start Command: `cd backend && gunicorn backend.wsgi:application`
   - Root Directory: `backend`

### Настройка CORS на бэкенде

Обновите `backend/backend/settings.py`:

```python
# Добавьте URL вашего Vercel приложения в CORS_ALLOWED_ORIGINS
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "https://your-app.vercel.app",  # Ваш Vercel URL
]
```

## 🌐 Шаг 2: Развертывание Frontend на Vercel

### Метод 1: Через веб-интерфейс Vercel (рекомендуется для первого раза)

1. **Подготовка проекта**
   - Убедитесь, что ваш код залит в Git репозиторий (GitHub, GitLab, Bitbucket)

2. **Импорт проекта в Vercel**
   - Перейдите на [vercel.com](https://vercel.com)
   - Нажмите "Add New..." → "Project"
   - Импортируйте ваш репозиторий

3. **Настройка проекта**
   - **Framework Preset**: Next.js (определяется автоматически)
   - **Root Directory**: `frontend` (важно!)
   - **Build Command**: `npm run build` (или оставьте по умолчанию)
   - **Output Directory**: `.next` (по умолчанию)

4. **Настройка переменных окружения**
   В разделе "Environment Variables" добавьте:
   - `NEXT_PUBLIC_API_URL` = `https://your-backend-url.com/api`
     (Замените `your-backend-url.com` на URL вашего развернутого Django API)

5. **Деплой**
   - Нажмите "Deploy"
   - Дождитесь завершения сборки
   - После успешного деплоя вы получите URL вашего приложения

### Метод 2: Через Vercel CLI

1. **Установка Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **Авторизация**
   ```bash
   vercel login
   ```

3. **Переход в папку frontend**
   ```bash
   cd frontend
   ```

4. **Деплой**
   ```bash
   vercel
   ```
   
   При первом деплое вам будет предложено:
   - Link to existing project? → `N` (если это первый раз)
   - Project name → введите название проекта
   - Directory → `./` (или оставьте по умолчанию)

5. **Настройка переменных окружения**
   ```bash
   vercel env add NEXT_PUBLIC_API_URL
   ```
   Введите значение: `https://your-backend-url.com/api`

6. **Повторный деплой с переменными**
   ```bash
   vercel --prod
   ```

## ⚙️ Шаг 3: Дополнительные настройки

### Обновление next.config.js для production

Файл `next.config.js` уже настроен для поддержки изображений из внешних источников. Убедитесь, что в `remotePatterns` добавлен домен вашего бэкенда:

```javascript
images: {
  remotePatterns: [
    {
      protocol: 'https',
      hostname: 'your-backend-domain.com', // Ваш бэкенд домен
      pathname: '/media/**',
    },
  ],
}
```

### Обновление URL в settings.py бэкенда

После получения URL от Vercel, обновите `backend/backend/settings.py`:

```python
ALLOWED_HOSTS = ['your-backend-domain.com', 'your-app.vercel.app']
CORS_ALLOWED_ORIGINS = [
    "https://your-app.vercel.app",
]
```

## 🔄 Непрерывное развертывание (CI/CD)

Vercel автоматически настраивает непрерывное развертывание:
- При каждом push в основную ветку → автоматический деплой в production
- При создании pull request → создается preview deployment

## ✅ Проверка после деплоя

1. Откройте ваш Vercel URL
2. Проверьте, что страницы загружаются
3. Проверьте работу API (попробуйте загрузить список товаров)
4. Проверьте авторизацию и другие функции

## 🐛 Решение проблем

### Ошибка: "API URL not found"
- Убедитесь, что переменная окружения `NEXT_PUBLIC_API_URL` установлена в Vercel
- Перезапустите деплой после добавления переменных

### Ошибка CORS
- Убедитесь, что URL вашего Vercel приложения добавлен в `CORS_ALLOWED_ORIGINS` на бэкенде
- Проверьте, что бэкенд разрешает запросы с вашего домена

### Изображения не загружаются
- Проверьте настройки `remotePatterns` в `next.config.js`
- Убедитесь, что `MEDIA_URL` правильно настроен на бэкенде

## 📚 Полезные ссылки

- [Документация Vercel](https://vercel.com/docs)
- [Документация Next.js Deployment](https://nextjs.org/docs/deployment)
- [Railway Documentation](https://docs.railway.app)
- [Render Documentation](https://render.com/docs)

## 💡 Примечания

- **Важно**: Все переменные окружения, начинающиеся с `NEXT_PUBLIC_`, доступны в браузере
- **Безопасность**: Никогда не храните секретные ключи в `NEXT_PUBLIC_*` переменных
- **Бэкенд**: Django бэкенд НЕ может быть развернут на Vercel (Vercel только для frontend/Node.js)

