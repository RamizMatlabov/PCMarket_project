# 🔧 Исправление ошибки CORS

## ❌ Проблема
Вы видите ошибки в консоли браузера:
```
CORS error
Access to fetch at 'https://...' from origin 'https://...' has been blocked by CORS policy
```

## ✅ Решение

### Шаг 1: Обновите настройки CORS на бэкенде

Файл `backend/backend/settings.py` уже обновлён! Теперь он поддерживает все домены Vercel автоматически.

**Что было добавлено:**
```python
CORS_ALLOWED_ORIGIN_REGEXES = [
    r"^https://.*\.vercel\.app$",
]
```

Это означает, что **любой** домен вида `*.vercel.app` будет разрешён автоматически.

### Шаг 2: Закоммитьте и запушьте изменения

1. Откройте терминал в папке проекта
2. Выполните команды:

```bash
git add backend/backend/settings.py
git commit -m "Fix CORS: Add support for all Vercel domains"
git push
```

3. Render.com автоматически обнаружит изменения и пересоберёт проект
4. Подождите 1-2 минуты

### Шаг 3: Проверьте работу

1. Откройте ваш сайт на Vercel
2. Нажмите `F12` → вкладка **Console**
3. Обновите страницу (`F5`)
4. Ошибки CORS должны исчезнуть ✅

---

## 🔍 Если ошибка всё ещё есть

### Вариант 1: Проверьте точный URL фронтенда

1. Откройте ваш проект на Vercel
2. Посмотрите точный URL в адресной строке
3. Если это не `*.vercel.app`, а другой домен — добавьте его вручную:

В `backend/backend/settings.py`:
```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",
    "https://pcmarket-project.onrender.com",
    "https://your-exact-frontend-url.com",  # Добавьте ваш точный URL
]
```

### Вариант 2: Временно разрешить все домены (только для теста!)

Если ничего не помогает, временно используйте:

```python
# ВРЕМЕННО для теста!
CORS_ALLOW_ALL_ORIGINS = True
CORS_ALLOW_CREDENTIALS = True
```

⚠️ **Внимание**: Это разрешит запросы с любых доменов. Используйте только для теста!

После того как убедитесь, что всё работает, вернитесь к нормальным настройкам.

---

## 📋 Чеклист

- [ ] Файл `backend/backend/settings.py` обновлён
- [ ] Изменения закоммичены и запушены в Git
- [ ] Render.com пересобрал проект (проверьте логи)
- [ ] Ошибки CORS исчезли в консоли браузера
- [ ] Сайт работает корректно

---

## 💡 Почему это работает?

Регулярное выражение `r"^https://.*\.vercel\.app$"` означает:
- `^https://` — начинается с https://
- `.*` — любые символы (любой поддомен)
- `\.vercel\.app` — заканчивается на .vercel.app
- `$` — конец строки

Таким образом, все домены вида:
- `https://pcmarket-project.vercel.app`
- `https://pcmarket-project-git-main.vercel.app`
- `https://pcmarket-project-abc123.vercel.app`
- и т.д.

будут автоматически разрешены!

---

## ✅ Готово!

После выполнения всех шагов ошибки CORS должны исчезнуть, и сайт будет работать корректно.

