# 📋 Инструкция: Как исправить проблему "Сервер не доступен"

## ❌ Проблема
Ваш фронтенд на Vercel пытается подключиться к `localhost:8000` вместо бэкенда на Render.com.

## ✅ Решение (выполните шаги ниже)

### Шаг 1: Найдите URL вашего бэкенда на Render.com

1. Откройте сайт [Render.com](https://render.com)
2. Войдите в свой аккаунт
3. Найдите ваш бэкенд сервис (Django backend)
4. Скопируйте URL сервиса
   - Пример: `https://pcmarket-project.onrender.com`
   - ⚠️ **Важно**: URL должен быть без `/` в конце!

---

### Шаг 2: Добавьте переменную окружения в Vercel

1. Откройте сайт [Vercel.com](https://vercel.com)
2. Войдите в свой аккаунт
3. Найдите ваш проект (фронтенд)
4. Нажмите на название проекта
5. Перейдите в раздел **Settings** (в верхнем меню)
6. В левом меню выберите **Environment Variables**
7. Нажмите кнопку **Add New** (или **Add**)
8. Заполните форму:
   - **Key (Имя)**: `NEXT_PUBLIC_API_URL`
   - **Value (Значение)**: Вставьте URL вашего бэкенда из Шага 1
     - Пример: `https://pcmarket-project.onrender.com`
9. Выберите все окружения (галочки):
   - ✅ Production
   - ✅ Preview  
   - ✅ Development
10. Нажмите **Save**

---

### Шаг 3: Пересоберите проект на Vercel

1. В Vercel перейдите в раздел **Deployments**
2. Найдите последний деплой (самый верхний)
3. Нажмите на три точки (⋮) справа от деплоя
4. Выберите **Redeploy**
5. Подтвердите пересборку
6. Подождите 1-2 минуты, пока проект пересоберется

---

### Шаг 4: Проверьте настройки CORS на бэкенде

Убедитесь, что ваш Django бэкенд разрешает запросы с фронтенда на Vercel.

1. Откройте файл `backend/backend/settings.py`
2. Найдите настройки `CORS_ALLOWED_ORIGINS`
3. Добавьте URL вашего фронтенда на Vercel:

```python
CORS_ALLOWED_ORIGINS = [
    "https://your-frontend-name.vercel.app",  # Замените на ваш реальный URL Vercel
    "http://localhost:3000",  # для локальной разработки
]
```

4. Если файл не найден или настройки отсутствуют, добавьте в начало файла:

```python
from corsheaders.defaults import default_headers

CORS_ALLOWED_ORIGINS = [
    "https://your-frontend-name.vercel.app",  # Замените на ваш реальный URL Vercel
    "http://localhost:3000",
]
```

5. Сохраните файл и перезапустите бэкенд на Render.com (если нужно)

---

### Шаг 5: Проверьте работу

1. Откройте ваш сайт на Vercel
2. Нажмите `F12` (откроется консоль разработчика)
3. Перейдите на вкладку **Console**
4. Обновите страницу (`F5`)
5. Проверьте, что в консоли нет ошибок "localhost:8000"
6. Если видите запросы к Render.com — всё работает! ✅

---

## 🔍 Как проверить, что всё настроено правильно?

### В консоли браузера (F12 → Console) вы должны видеть:
- ✅ Запросы к URL типа `https://your-backend.onrender.com/api/...`
- ❌ НЕ должно быть запросов к `http://localhost:8000`

### Если всё ещё не работает:

1. **Проверьте URL бэкенда**:
   - Откройте URL бэкенда в браузере
   - Должна открыться страница Django (или API документация)
   - Если не открывается — бэкенд не запущен на Render.com

2. **Проверьте переменную окружения в Vercel**:
   - Settings → Environment Variables
   - Убедитесь, что `NEXT_PUBLIC_API_URL` существует
   - Проверьте, что значение правильное (без `/` в конце)

3. **Проверьте CORS**:
   - В консоли браузера могут быть ошибки CORS
   - Если есть — добавьте URL фронтенда в `CORS_ALLOWED_ORIGINS` на бэкенде

---

## 📞 Нужна помощь?

Если проблема не решена, проверьте:
- [DEPLOYMENT.md](./DEPLOYMENT.md) — подробная инструкция
- [QUICK_FIX.md](./QUICK_FIX.md) — быстрое решение

---

## ✅ Чеклист выполнения

- [ ] Нашел URL бэкенда на Render.com
- [ ] Добавил переменную `NEXT_PUBLIC_API_URL` в Vercel
- [ ] Выбрал все окружения (Production, Preview, Development)
- [ ] Пересобрал проект на Vercel
- [ ] Настроил CORS на бэкенде
- [ ] Проверил работу сайта
- [ ] В консоли браузера нет ошибок

**После выполнения всех пунктов проблема должна быть решена!** 🎉

